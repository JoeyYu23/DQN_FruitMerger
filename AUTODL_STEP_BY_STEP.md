# AutoDL 详细部署步骤（图文教程）

手把手教你在AutoDL上部署DQN_FruitMerger项目

---

## 准备工作

### 需要的信息
- GitHub仓库：`https://github.com/JoeyYu23/DQN_FruitMerger.git`
- 手机号（用于注册AutoDL）
- 至少50元充值（实际使用可能只需要10-20元）

---

## 第一步：注册AutoDL账号（5分钟）

### 1.1 访问AutoDL官网

在浏览器打开：**https://www.autodl.com**

### 1.2 注册账号

1. 点击右上角"注册"或"登录"
2. 使用手机号注册
3. 输入验证码
4. 设置密码

### 1.3 实名认证

注册后需要实名认证（必需步骤）：
1. 进入"个人中心"
2. 点击"实名认证"
3. 输入身份证信息
4. 等待审核（通常几分钟内完成）

### 1.4 充值

1. 点击"充值"
2. 建议充值50-100元（够用很久）
3. 支持支付宝、微信支付

---

## 第二步：创建GPU实例（3分钟）

### 2.1 进入算力市场

1. 登录后，点击左侧菜单"算力市场"
2. 或直接访问：https://www.autodl.com/market/list

### 2.2 选择GPU配置

**推荐配置（根据你的需求）：**

#### 配置选项：

**GPU选择**（重要！）
```
推荐1: RTX 3080 Ti (12GB)  - 性价比最高
推荐2: RTX 3090 (24GB)     - 显存更大
推荐3: RTX 4090 (24GB)     - 性能最强（价格贵）

价格范围: 1.5-3元/小时
```

**筛选条件：**
- 显存：≥ 12GB
- 状态：可租用
- 地区：选择延迟低的（通常选"华东"或"华北"）

**镜像选择**（重要！）
```
选择: PyTorch 镜像
具体选择: PyTorch 2.1.0 / Python 3.10 / CUDA 11.8

或者选择包含以下关键词的镜像：
- PyTorch 2.x
- Python 3.10 或 3.8-3.11
- CUDA 11.x
```

**存储配置：**
```
系统盘: 50GB（默认，足够用）
数据盘: 0GB（不需要）
```

### 2.3 创建实例

1. 选好配置后，点击"租用"
2. 确认配置信息
3. 点击"立即创建"
4. 等待实例启动（约30秒-1分钟）

### 2.4 实例创建成功

看到实例状态变为"运行中"，显示绿色标识

---

## 第三步：连接到实例（2种方法）

### 方法A：使用JupyterLab（推荐，简单）

#### 3.1 打开JupyterLab

1. 在"容器实例"页面，找到你的实例
2. 点击"JupyterLab"按钮
3. 会在新标签页打开JupyterLab界面

#### 3.2 打开终端

在JupyterLab界面：
1. 点击底部的"Terminal"图标
2. 或者：Launcher → Other → Terminal
3. 会打开一个黑色的终端窗口

**你现在已经连接到服务器了！**

### 方法B：使用SSH（适合熟练用户）

#### 3.1 获取SSH连接信息

1. 在实例页面，点击"SSH"按钮
2. 会显示类似这样的命令：
   ```bash
   ssh -p 12345 root@region-1.autodl.com
   密码: xxxxxxxx
   ```

#### 3.2 在Mac终端连接

1. 打开Mac的Terminal（终端）
2. 复制SSH命令并执行
3. 输入密码
4. 连接成功

---

## 第四步：部署项目（5分钟）

### 4.1 方式一：使用一键部署脚本（最简单）

在终端中依次执行：

```bash
# 1. 进入root目录
cd /root

# 2. 下载一键部署脚本
wget https://raw.githubusercontent.com/JoeyYu23/DQN_FruitMerger/main/autodl_quick_deploy.sh

# 3. 添加执行权限
chmod +x autodl_quick_deploy.sh

# 4. 运行部署脚本
./autodl_quick_deploy.sh
```

**脚本会自动完成：**
- ✅ 从GitHub克隆代码
- ✅ 检查GPU和CUDA环境
- ✅ 创建Python虚拟环境
- ✅ 安装PyTorch 2.1.2（匹配CUDA 11.8）
- ✅ 安装所有项目依赖
- ✅ 运行环境测试

**预计时间：3-5分钟**

### 4.2 方式二：手动部署（分步执行）

如果一键脚本失败，可以手动执行：

#### 步骤1: 克隆代码
```bash
cd /root
git clone https://github.com/JoeyYu23/DQN_FruitMerger.git
cd DQN_FruitMerger
```

#### 步骤2: 运行部署脚本
```bash
chmod +x deploy_server.sh
./deploy_server.sh
```

#### 步骤3: 验证环境
```bash
source venv/bin/activate
python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA:', torch.cuda.is_available())"
```

应该看到：
```
PyTorch: 2.1.2+cu118
CUDA: True
```

---

## 第五步：开始训练（立即可用）

### 5.1 激活虚拟环境

```bash
cd /root/DQN_FruitMerger
source venv/bin/activate
```

### 5.2 安装PaddlePaddle（DQN训练需要）

```bash
pip install paddlepaddle-gpu==2.5.1.post118 \
    -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
```

**注意：** 当前项目使用PaddlePaddle框架。PyTorch版本还在开发中。

### 5.3 开始训练

**方式1：直接训练（不推荐）**
```bash
python DQN.py
```
缺点：SSH断开会中断训练

**方式2：使用tmux后台运行（强烈推荐）**
```bash
# 创建tmux会话
tmux new -s train

# 在tmux中运行训练
source venv/bin/activate
python DQN.py

# 分离会话：按 Ctrl+B，然后按 D
# 这样即使关闭SSH，训练也会继续
```

**重新连接tmux：**
```bash
tmux attach -s train
```

**方式3：使用nohup后台运行**
```bash
source venv/bin/activate
nohup python DQN.py > train.log 2>&1 &

# 查看日志
tail -f train.log
```

---

## 第六步：监控训练

### 6.1 监控GPU使用情况

**实时监控：**
```bash
watch -n 1 nvidia-smi
```

按 `Ctrl+C` 退出监控

**单次查看：**
```bash
nvidia-smi
```

应该看到类似：
```
+-----------------------------------------------------------------------------+
| NVIDIA-SMI 520.xx       Driver Version: 520.xx       CUDA Version: 11.8    |
|-------------------------------+----------------------+----------------------+
| GPU  Name        Persistence-M| Bus-Id        Disp.A | Volatile Uncorr. ECC |
|   0  NVIDIA GeForce ... Off  | 00000000:00:0E.0 Off |                  N/A |
| 30%   45C    P2   120W / 350W |  10240MiB / 12288MiB |     95%      Default |
+-------------------------------+----------------------+----------------------+
```

**关键指标：**
- GPU利用率：应该在80-100%
- 显存使用：会逐渐增加到几GB
- 温度：正常在40-80°C

### 6.2 查看训练日志

```bash
# 实时查看
tail -f training.log

# 查看最后100行
tail -n 100 training.log
```

### 6.3 启动TensorBoard监控

```bash
# 后台启动TensorBoard
source venv/bin/activate
nohup tensorboard --logdir=./logs --host=0.0.0.0 --port=6006 > tensorboard.log 2>&1 &
```

**访问TensorBoard：**
1. 在AutoDL控制台，找到你的实例
2. 查看"自定义服务"端口映射
3. 如果没有配置6006端口，需要先配置：
   - 点击实例的"更多" → "端口映射"
   - 添加：6006 → http
4. 访问显示的TensorBoard链接

---

## 第七步：训练完成后操作

### 7.1 下载训练结果

**方式1：通过JupyterLab下载（最简单）**
1. 在JupyterLab的文件浏览器中
2. 找到 `weights/` 或 `*.pdparams` 文件
3. 右键点击 → Download

**方式2：使用scp下载到Mac**

在Mac终端执行：
```bash
# 下载模型文件
scp -P <端口号> -r root@<服务器地址>:/root/DQN_FruitMerger/weights ./models/

# 下载训练日志
scp -P <端口号> root@<服务器地址>:/root/DQN_FruitMerger/training.log ./logs/
```

端口号和服务器地址可以在AutoDL的"SSH"按钮中查看

**方式3：推送到GitHub**

在AutoDL服务器上：
```bash
cd /root/DQN_FruitMerger
git add weights/
git commit -m "Add trained model"
git push origin main
```

然后在Mac上：
```bash
cd /Users/ycy/Downloads/DQN_FruitMerger
git pull origin main
```

### 7.2 关闭实例（重要！）

**训练完成后立即关闭实例，避免继续计费！**

1. 回到AutoDL控制台
2. 找到你的实例
3. 点击"关机"或"释放"按钮
4. 确认关闭

**费用核算：**
- 进入"账单明细"查看实际费用
- 关机后不再计费

---

## 常见问题和解决方案

### Q1: git clone速度很慢

**解决方案1：使用ZIP下载**
```bash
cd /root
wget https://github.com/JoeyYu23/DQN_FruitMerger/archive/refs/heads/main.zip
unzip main.zip
mv DQN_FruitMerger-main DQN_FruitMerger
cd DQN_FruitMerger
```

**解决方案2：使用镜像加速**
```bash
git clone https://ghproxy.com/https://github.com/JoeyYu23/DQN_FruitMerger.git
```

### Q2: pip安装很慢

**使用清华镜像源：**
```bash
pip install <package> -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3: CUDA Out of Memory（显存不足）

**解决方案：**
1. 检查是否有其他程序占用GPU
   ```bash
   nvidia-smi
   ```
2. 减小batch_size（修改代码中的参数）
3. 清理GPU缓存
   ```python
   import torch
   torch.cuda.empty_cache()
   ```

### Q4: SSH连接断开

**不影响训练，如果你用了tmux：**
```bash
# 重新SSH连接后
tmux attach -s train
```

### Q5: 训练意外停止

**检查：**
```bash
# 查看进程
ps aux | grep python

# 查看日志
tail -n 100 training.log
```

**重新开始：**
```bash
cd /root/DQN_FruitMerger
source venv/bin/activate
tmux new -s train
python DQN.py
```

### Q6: 环境测试失败

**重新运行部署脚本：**
```bash
cd /root/DQN_FruitMerger
./deploy_server.sh
```

### Q7: 找不到某个文件

**确保在正确目录：**
```bash
pwd  # 应该显示 /root/DQN_FruitMerger
ls -la  # 列出所有文件
```

---

## 快速命令参考卡

### 常用命令
```bash
# 连接实例
ssh -p <端口> root@<地址>

# 进入项目
cd /root/DQN_FruitMerger

# 激活环境
source venv/bin/activate

# 开始训练（tmux）
tmux new -s train
python DQN.py
# Ctrl+B, D 分离

# 重连tmux
tmux attach -s train

# 监控GPU
watch -n 1 nvidia-smi

# 查看日志
tail -f training.log

# 查看进程
ps aux | grep python

# 停止训练
pkill -f DQN.py
```

---

## 训练时间和费用估算

### 基于 RTX 3080 Ti @ ¥2/小时

| 训练任务 | Episodes | 预计时间 | 费用 | 最终模型大小 |
|---------|----------|---------|------|------------|
| 快速测试 | 100 | 30分钟 | ¥1 | ~200KB |
| 小规模训练 | 500 | 2-3小时 | ¥4-6 | ~200KB |
| 标准训练 | 2000 | 6-8小时 | ¥12-16 | ~200KB |
| 完整训练 | 5000 | 15-20小时 | ¥30-40 | ~200KB |
| 超长训练 | 10000 | 30-40小时 | ¥60-80 | ~200KB |

### 省钱技巧
1. ✅ 训练完立即关机
2. ✅ 使用tmux防止断连
3. ✅ 选择凌晨时段（部分时段有优惠）
4. ✅ 批量训练多个模型
5. ✅ 使用checkpoint断点续训

---

## 成功标志

### 部署成功的标志：
```
✓ Git clone成功
✓ 虚拟环境创建成功
✓ PyTorch安装成功
✓ CUDA可用（torch.cuda.is_available() = True）
✓ 环境测试通过
```

### 训练成功的标志：
```
✓ GPU利用率 > 80%
✓ 训练日志持续更新
✓ Episode数字不断增加
✓ Score逐渐提升
```

---

## 总结

### 完整流程回顾：

1. **注册AutoDL** → 实名认证 → 充值（5分钟）
2. **创建实例** → 选GPU + 镜像 → 启动（2分钟）
3. **连接实例** → JupyterLab或SSH（1分钟）
4. **部署项目** → 一键脚本或手动（5分钟）
5. **开始训练** → tmux + python DQN.py（立即）
6. **监控训练** → nvidia-smi + 日志
7. **完成后** → 下载模型 + 关机

**总耗时：约15分钟即可开始训练**

---

**现在你已经掌握了完整的部署流程！** 🎉

如有问题，查看：
- 本教程
- `GITHUB_AUTODL_GUIDE.md`
- `SERVER_DEPLOYMENT_GUIDE.md`

祝训练顺利！🚀
